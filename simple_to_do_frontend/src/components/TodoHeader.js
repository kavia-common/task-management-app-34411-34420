/**
 * Simple header component for the To-Do app.
 * Presentational only; App renders it within the gradient header bar.
 */
// PUBLIC_INTERFACE
export default function TodoHeader() {
  /** This component is intentionally minimal and not used directly as header is in App */
  return null;
}
